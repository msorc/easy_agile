<% content_for :title, "#{@project} | Projects" %>
<% content_for :h2, h(@project) %>
<% body_classes << 'guidance' %>

<%= simple_format h(@project.description) %>

<h2>No Stories</h2>
<p>
You should start by <%= link_to 'creating a story', [:new, @project, :story] %>.
</p>
