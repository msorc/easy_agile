<p>
<%= label_tag :project_id %>
<%= select_tag(:project_id,
               options_from_collection_for_select(User.current.projects,
                                                  :id,
                                                  :to_s,
                                                  params[:project_id].to_i)) %>
</p>
