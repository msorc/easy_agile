<% content_for :title, "New Story" %>
<% content_for :h1, "New Story" %>
<% breadcrumbs link_to(h(@project), @project), 'New Story' %>
<% form_for @story, :url => project_stories_path do |form| %>
  <%= form.error_messages %>
  <%= render form %>
  <p class="submit">
  <%= form.submit 'Create Story' %>
  </p>
<% end %>
